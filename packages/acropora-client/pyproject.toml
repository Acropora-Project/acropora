[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "acropora-client"
version = "0.1.0"
description = "Acropora client SDK — query the CORAL network from your application"
readme = "README.md"
license = { text = "MIT" }
requires-python = ">=3.12"
authors = [{ name = "Acropora Project Contributors" }]
keywords = ["acropora", "coral-network", "reef-protocol", "spatiotemporal", "federated"]
classifiers = [
    "Development Status :: 2 - Pre-Alpha",
    "License :: OSI Approved :: MIT License",
    "Programming Language :: Python :: 3.12",
]
dependencies = [
    "httpx>=0.27",
    "websockets>=12.0",
    "pynacl>=1.5",
    "pyarrow>=16.0",
]

[project.urls]
Homepage = "https://acropora.network"
Repository = "https://github.com/acropora-project/acropora"
Documentation = "https://acropora.dev"

[tool.hatch.build.targets.wheel]
packages = ["src/acropora_client"]

[dependency-groups]
dev = [
    "pytest>=8.0",
    "pytest-asyncio>=0.23",
    "mypy>=1.10",
]